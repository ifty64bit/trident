function Header() {
    return (
        <header className="sticky left-0 right-0 top-0 flex justify-between items-center h-20 bg-primary text-primary-foreground">
            Header
        </header>
    );
}

export default Header;
